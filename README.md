# vite-public-dir-warning-issue

Running `yarn start` will show this warning, even though the publicDir has been renamed to 'static'.

```
files in the public directory are served at the root path.
Instead of /public/foo, use /foo.
```
